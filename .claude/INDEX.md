<!-- Search index: recurring concern -> where it lives. Consult before searching (index_first); append a stable location when you confirm one. Keeps searches targeted instead of scanning the tree or spawning a search agent. Seeded from CLAUDE.md; grow it as you work. -->

# Index

## Submodules (work in the subdir and push there, not the root)
- `discord-server` — Python 3.14+ discord.py, PostgreSQL. Entry `main.py` → `asyncio.run(main())`. `CommandHandler` singleton over `app_commands.CommandTree`; `discord_client.py`; `service_loader.py`/`service_handler.py`. Features under `features/`: account, action_log, broadcast, member, parties, tickets, user_keys.
- `discord-utils` — Python 3.14+ discord.py, PostgreSQL + Valkey.
- `discord-event` — Python 3.14+ discord.py, MongoDB.
- `api-backend` — Python 3.14+ FastAPI + Gunicorn, PostgreSQL + Valkey. Routers: assets, auth, badges, clan, config, content, events, members, music, parties, ranking, staff, surveys, ccdispatch. Migrations `alembic/versions/`. Tests `app/tests/` (anyio, dry-run/integration; every endpoint has one).
- `web-app` — TypeScript, React 19, Bun. Two separate Bun servers: dev/HMR `src/index.tsx` (`bun dev`/`start`) and prod `src/prod-server.ts` (Docker CMD, OG-meta SPA injection). Shared `/embed/*` OG-image + fixtures + `_preview` handling in `src/embed/routes.ts` (`handleEmbedRoutes`); cards in `src/embed/*.tsx` (satori). Routes `src/routes/` (TanStack Router); UI Shadcn/ui. Design baseline: `/about`, `/rules`, `/home`.
- `osrs-cache-service` — Python 3.14+ FastAPI, dedicated `cache-postgres`. JS5 decoder registry — see `osrs-cache-service/CLAUDE.md`.

## Config / infra / tooling
- `docker-compose.yml` (+ `.override.yml`, `.traefik.yml`) — PostgreSQL 17, MongoDB 8, Valkey 8; `foundry` bridge network.
- Secrets via Infisical; keep env changes in the example file + the compose files.
- Python: `uv` only (`uv add`, `uv run ruff/pytest/pyright`). Web: `bun`.
- `.claude/intel/` — PAG corpus (grammar, authoring guides, templates); its own index at `.claude/intel/INDEX.md`. Consult before writing/editing any PAG document.
- Deriving what a change must test (surfaces, not just layers): `.claude/intel/reference_test_surface_coverage.md`; the run/ship policy is `.claude/rules/testing.md`.
- Codebase overview report: `/codebase-stats` (`.claude/commands/codebase-stats.md`) runs `.claude/scripts/codebase_stats/` - stdlib-only Python, project-agnostic, writes `CODEBASE-STATS.md` on the five axes of `reference_pattern_ontology.md`. Modules: `walk.py` (scan), `gitinfo.py` (repos+submodules), `packages.py` (manifests: package.json/pyproject PEP621+poetry/Cargo.toml/go.mod, dep graph), `sec_entities.py` + `sec_analysis.py` (axes), `md.py` (tables/bars). Sections self-omit when a project has no data for them.
- `previews/` — standalone HTML deliverables (mockups, visual overviews, surface previews). Every HTML deliverable lands here, never the scratchpad; see [[no-artifacts]].
- `designs/` — specs and stage trackers, e.g. `MUSIC_BOTS.md`. `fixtures/` — cross-service contract fixtures both sides assert against.

## Reference repos (D:\claude-git-references)
- `wise-old-man-repo` — WiseOldMan (github.com/wise-old-man/wise-old-man). Used with `add-metric` skill for WOM metric additions.
- `clansocket-osrs-cache-extractor-repo` — clansocket OSRS cache extractor. Ground-truth for `osrs-cache-service` 2D item-icon rendering (model lighting/camera/HSL, `app/models/`).
- `discord.py-repo` — discord.py (github.com/Rapptz/discord.py). Library source for the discord-server/discord-utils/discord-event submodules (commands, app_commands, UI, intents).
- `cache-mediawiki-repo` — osrs-wiki cache-mediawiki. OSRS cache -> MediaWiki scripts; reference for `osrs-cache-service` cache decode/definition extraction.
- `osrs-wiki-maps-repo` — osrs-wiki maps tooling. Generates OSRS wiki map images; reference for `osrs-cache-service` map render/tiling.
- `wom.py-repo` — wom.py (github.com/jonxslays/wom.py). Async Python WiseOldMan API client; reference for WOM endpoints/models (pairs with `wise-old-man-repo`, `add-metric` skill).
- `runelite-repo` — RuneLite (github.com/runelite/runelite). OSRS client source; cache format + plugin API reference for the api<->runelite seam.
- `vocard-repo` — Vocard (github.com/ChocoMeow/Vocard). discord.py + Lavalink music bot; reference for the discord-utils music orchestration plan (`voicelink/` player/queue/filters, `cogs/` commands, node pooling, web dashboard IPC).
- `lavalink-repo` — Lavalink (github.com/lavalink-devs/Lavalink). Audio node server. **The full lavalink.dev docs site is in `docs/`** (`docs/api/rest.md`, `docs/api/websocket.md`, `docs/configuration/config/environment-variables.md`, `docs/plugins.md`) — read those instead of fetching lavalink.dev. Multi-bot proof: `LavalinkServer/src/main/java/lavalink/server/io/SocketServer.kt` + `SocketContext.kt`. Reference config: `LavalinkServer/application.yml.example`.
- `wavelink-repo` — wavelink (github.com/PythonistaGuild/Wavelink). Python Lavalink v4 client used by discord-utils music. `wavelink/node.py` (`Pool` class-level registry, per-`Node` client/players, `User-Id` header), `wavelink/player.py` (`Player.__init__(nodes=...)` — must be passed explicitly, see MUSIC_BOTS.md).
- `youtube-source-repo` — youtube-source (github.com/lavalink-devs/youtube-source). Lavalink YouTube plugin replacing the deprecated built-in source; `README.md` has the client list, oauth and `poToken` notes.
- `lavasrc-repo` — LavaSrc (github.com/topi314/LavaSrc). Lavalink plugin for Spotify/Apple/Deezer metadata with audio mirroring; `README.md` has the full `plugins.lavasrc` config and the provider/ISRC semantics.

<!-- append below: <concern> -> <path> as you discover stable locations -->- API reference / OpenAPI metadata (api-backend): everything shaping the Scalar page at `/docs` is in `app/docs/` — `description.py` (markdown intro), `tags.py` (`TAGS_METADATA` + `TAG_GROUPS` -> `x-tagGroups`), `schema.py` (`SERVERS`, `install_openapi_customization`), `reference.py` (renderer branding, `persist_auth`), `responses.py` (reusable 401/403/404/502/503, attached via `APIRouter(responses=...)`). Security schemes are declared in `app/dependencies.py` (`bearer_scheme`/`clan_key_scheme`/`metrics_key_scheme`, all `auto_error=False`). Regen artifact: `uv run python scripts/generate_openapi.py` then web `bun run gen:api-types`; guarded by `app/tests/test_openapi_contract.py` + `test_openapi_metadata.py` + `test_docs.py`.
- Service version + build provenance: `<module>/pyproject.toml` (or `web-app/package.json`) is the only source; `app/version.py` in api-backend and osrs-cache-service reads it via `tomllib`. `GET /version` lives in `api-backend/app/routers/meta.py` (alongside `/health`) and `osrs-cache-service/app/api/routers/meta.py`. `GIT_SHA`/`BUILD_TIME` are Dockerfile build args wired through `docker-compose.yml`. Per-module `CHANGELOG.md`; CI bump gate is the `version` job in each module's `.github/workflows/test.yml` (exempts only `.github/**` and `*.md`). Rules for when/what/how to bump: `.claude/rules/versioning.md` (PAG POLICY).
- Loot tables + EHP/EHB reference data (api-backend): read endpoints `GET /reference/loot/sources[/{slug}]`, `/reference/loot/items/{item_id}` (reverse lookup), `/reference/rates?kind=ehp|ehb` in `app/routers/reference/`. Two scheduled lifespan services: `app/services/loot_tables/` (daily, OSRS Wiki `action=parse` wikitext -> `{{DropsLine}}` parser in `_drops.py`; source catalog from `DEFAULT_BOSS_METRICS` + `data/sources.toml` overrides; item-id via osrs-cache-service `/items/names`) and `app/services/efficiency_rates/` (hourly, WOM `/efficiency/rates?type=ironman`, EHB flat `[{boss,rate}]` / EHP tiered `[{skill,methods[],bonuses}]`). Models `app/db/models/reference_data.py` (loot_sources, loot_drops, efficiency_rates), migration `0054`. WOM efficiency client: `app/services/http/wom_efficiency.py`; wiki content fetch: `OsrsWikiContentHandler` in `app/services/http/wiki.py`. Web viewer (read-only): Control Panel -> "Reference Data" page `web-app/src/routes/members/config/reference.tsx` (pageId `staff.reference-data`, in `controlPanelPages.ts`), radix Tabs Bosses/Activities/Clues/Rates; components `web-app/src/components/reference/` (LootSourcesTab, DropTable, RatesTab); api `src/api/reference.ts` + hook `src/hooks/useReference.ts` + types `src/types/reference.ts`; drop-rate math in `src/lib/droprate.ts` (normalized 1/N rarity + per-kill/roll chance). Raids/Barrows/Colosseum/Wintertodt loot is remapped in `data/sources.toml` to their reward-chest wiki pages (all standard `{{DropsLine}}`); those sources carry `reward_kind="chest"` (LootSource col, migration 0056) so the viewer labels rarities as per-roll and leaves room to model point-scaling later. Mode variants (CM/HM/Expert) are in `loot_exclude` (loot only; they keep WOM EHB rates). Qualitative rarities stored as `LootDrop.rarity_text` (migration 0055).
- web-app SEO / per-route meta: injected server-side in `web-app/src/prod-server.ts` (`buildOgTags` = title/description/canonical/OG/Twitter/article meta; `buildStructuredData` = JSON-LD). JSON-LD builders (Organization/WebSite/BreadcrumbList/Article) in `web-app/src/lib/structured-data.ts`; ld+json scripts carry the CSP nonce. Sitemap `web-app/src/lib/sitemap.ts` (emits `<lastmod>` from each content entry's `updated_at`, sourced from the api-backend `GET /content/{page_type}/categories` tree — that endpoint returns an untyped `list[dict]`, so no openapi/schema.d.ts contract for those fields); robots `web-app/public/robots.txt`.
- Music bots (multi-bot Discord music): design + staged plan `designs/MUSIC_BOTS.md`. Orchestration in `discord-utils/music/` — `pool.py` (Valkey `SET NX` bot leases), `nodes.py` (one wavelink Node per bot; never let `Pool` choose), `session.py` + `transport.py` (playback, one `changed()` hook drives every panel redraw), `queue.py`/`state.py`/`activity.py`/`voice.py` (Valkey keys, all templated in `keys.py`), `resolve.py` (search + play-time audio lookup for any metadata-only track: Spotify mirrors and saved playlist rows take the same chain, so `played_source` is observable), `views/` (Components V2 panel, picker, queue view, playlist list), `commands/` (13 flat slash commands). Lavalink node: `lavalink/application.yml` + the `lavalink` service in `docker-compose.yml` (no plugins volume — see the comment there). Playlists live in api-backend `app/routers/music/` (user CRUD) + `bot.py` (service-key read surface for discord-utils), models `app/db/models/music.py`, migration `0057`; the discord-utils half is `music/playlists.py` (httpx client, `API_BACKEND_URL` + `METRICS_API_KEY`), `music/views/playlist_view.py` and `music/commands/playlists.py`. The seam is pinned by `fixtures/music_playlist.json`, asserted in `api-backend/app/tests/test_music.py`, `discord-utils/tests/test_api_contracts.py` and `integration/discord_e2e/tests/test_music_playlists.py`. Web control rides two Valkey pubsub channels named in `keys.py`: commands out from api-backend `app/routers/music/control.py` into `discord-utils/music/bridge.py` → `dispatch.py`, state notices back from `music/notify.py` into `app/services/music_live.py`, which reads the session via `app/routers/music/_live.py` and fans it out over the `GET /music/live` socket (`app/routers/music/live.py`, first-frame auth). Web surface: `web-app/src/components/music/` (panel built like `ControlPanel`, plus `MiniPlayer`), state in `src/context/MusicContext.tsx`, socket in `src/hooks/useMusicSocket.ts`. That seam is pinned by `fixtures/music_bridge.json`, asserted in `api-backend/app/tests/test_music_sessions.py`, `app/tests/integration/test_music_live_integration.py`, `discord-utils/tests/test_bridge_contract.py` and `integration/discord_e2e/tests/test_music_live.py`.
- Tile race teams + roster (api-backend `app/routers/tilerace/`): `tilerace_signups` is the persistent roster — `team_id` FK (`ON DELETE SET NULL`) + `is_captain` + `added_by_staff`; `tilerace_teams.members` was dropped in migration `0060`, rosters are derived by `group_by_team()`/`_serialize_team()` in `_helpers.py` (which also stamps `raids_kc` per member via `raids_kc_map()` off `player_snapshots.bosses`). Team generation `generate.py` (`POST .../teams/generate` with `{team_size, balance_raids_kc, raids_kc_threshold}`, `POST .../teams/reset`), pure algorithm in `_draft.py` (`target_sizes` = even sizing under a hard max, `greedy_draft` = strongest-first onto the lowest-average open team, `balance_raiders`, `pick_captain`, `RAID_METRICS`), team create/reconcile in `_teams_shape.py` (`TEAM_COLORS`, `reconcile_teams`). Staff roster CRUD `roster.py` + `_roster_helpers.py` (`POST .../roster`, `PATCH/DELETE .../roster/{discord_user_id}`); read-only pickers in `roster_lookup.py` (`GET .../roster/candidates`, `GET .../roster/{discord_user_id}/accounts`). Switching which linked RSN an entry races under is `_account_helpers.py` `apply_identity` (`account_id` repoints + rescores and keeps the team/captain badge; a raw `rsn` drops the account link), UI `builder/RsnSwitcher.tsx`. Web: `web-app/src/components/tilerace/builder/` — `TeamManager.tsx` (two-column board), `GenerateTeamsBar.tsx`, `TeamRosterCard.tsx`, `RosterMemberRow.tsx`, `AddRosterMember.tsx`; api `src/api/tilerace.ts`, hooks `src/hooks/useTilerace.ts`. Tests: `app/tests/test_tilerace_draft.py` (pure), `test_tilerace.py` (mocked), `app/tests/integration/test_tilerace_roster_integration.py` (real DB).
- Tile race public board masking (api-backend `app/routers/tilerace/_public_view.py`): `GET /tilerace/active` is the only unauthenticated tilerace route and the only masked one. `mask_cells()` applies fog of war server-side (a path cell beyond `max(team.position)` is cut to `{cell_x, cell_y, path_position}` BEFORE `_embed_cells` runs, so no fogged tile or modifier is ever fetched); `public_event()` builds the response from the `PUBLIC_SUMMARY_KEYS` allowlist, so a field added to `_serialize_summary` stays private until listed. Withheld: every `discord_*` id, `discord_permissions`, `teams[].pending_effects`, and `signups[]`. `teams[].members` is `{rsn, is_captain}`; the caller's own row rides as `my_signup` + `my_team_id` off `get_optional_user`, plus `signup_count`. Staff read the unmasked shape via `GET /tilerace/events/{id}` (`_full_event`). Web mirrors the split with `TileRacePublicEvent`/`TileRacePublicTeam`/`TileRaceBoardEvent` in `web-app/src/types/tilerace.ts`; `DiceRoller` takes `isMember` since the board no longer carries member ids. Tests: `app/tests/test_tilerace_public.py` (pure), `app/tests/integration/test_tilerace_public_integration.py` (real DB, uses the integration `anon_client` fixture). Doc: `designs/TILE_RACE.md` "The public board".
- Tile race Discord provisioning (api-backend `app/routers/tilerace/discord_setup.py` + `_discord_payload.py`, discord-server `features/tilerace/`): staff hit `POST /tilerace/events/{id}/discord/{setup,sync,teardown}`, which publishes a full-desired-shape command on Valkey `foundry:tilerace_discord`; discord-server's `TileRaceService` applies it to the guild and POSTs the ids back to `POST .../discord/result` (guarded by `verify_metrics_key`, the `verification-code` header). Bot module split: `service.py` (subscriber + dispatch), `provisioning.py` (`apply` is idempotent and writes ids into a sink so a part-way failure still reports, `teardown`, `teardown_team`), `guild_ops.py` (per-object ensure/delete + two-way role reconcile), `overwrites.py`, `naming.py`, `api_client.py`. Ids live on `tilerace_events.discord_{category,captains_role,captains_channel}_id` and `tilerace_teams.discord_{role,text_channel,voice_channel}_id` (migration `0061`, which also adds `tilerace_events.rolls_paused`). Every roster/name mutation calls `sync_if_provisioned`; team delete emits `teardown_team`. Event-wide channel permission toggles live on `tilerace_events.discord_permissions` (migration `0062`), canonical list in `_discord_perms.py` `TOGGLES` + the fixture's `permission_toggles`, patched via `PATCH .../discord/permissions` (which syncs), toggle -> discord flag tables in `discord-server/features/tilerace/perms.py`, applied in place by `perms.sync_role_overwrite` from the `ensure_*_channel` existing-object path (see [[tilerace-channel-permissions]]). Seam pinned by `fixtures/tilerace_discord.json`, asserted in `api-backend/app/tests/test_tilerace_discord.py`, `app/tests/integration/test_tilerace_discord_integration.py` (real Valkey pubsub) and `discord-server/tests/test_tilerace_contract.py`. Web: `web-app/src/components/tilerace/controls/` (`ControlsTab`, `DiscordCard`, `DiceSettingsCard`, `TeamPositions`) and `builder/TeamIdentityDialog.tsx` + `OsrsIconSearch.tsx`; board freshness for non-rollers is `src/hooks/useBoardRedraw.ts`.
- Tile race submissions + claim state (api-backend `app/routers/tilerace/submissions.py`, discord-server `features/tilerace/submissions/`): a roll is gated on a CLAIM, not staff approval - `controls.py` `_claimed()` accepts a `tilerace_tile_completions` row whose `status` is in `CLAIMED_STATUSES` (`claimed`/`approved`), and that row is written by `_submission_helpers.apply_claim_state()`, the single place the state machine lives (claim, rollback to the failed tile, restore to `tilerace_teams.furthest_position`). Requirement leaves are content-keyed by `_requirement_leaves.py` (`leaves()` / `is_satisfied()`, `item:{id}:{qty}` + `#n` for duplicates, `text:{sha1[:8]}`), so reordering a tile's items never orphans a submission. Lookups + review live in `_submission_lookup.py` (`submission_context`, `review_or_404`, `review_thread`, `drop_submission`); schemas in `submission_schema.py`; model `TileRaceSubmission` + migration `0063`. Bot half: `submissions/panel.py` (persistent Submit panel), `flow.py` (private thread per submission), `card.py` (leaf multi-select + Confirm), `proof.py` (Discord CDN -> WebP -> UploadThing, reusing `features/tickets/image_utils`), `review.py` (staff verdict `DynamicItem`, plain buttons carry the ids), `api.py` (service-key HTTP). The `#submissions` channel is provisioned like any other event object (`guild_ops.ensure_shared_channel` + `overwrites.shared_channel`, id on `tilerace_events.discord_submissions_channel_id`). Seams pinned by `fixtures/tilerace_submission.json` + `fixtures/tilerace_discord.json`, asserted in `api-backend/app/tests/test_tilerace_submissions.py`, `app/tests/integration/test_tilerace_submissions_integration.py`, `discord-server/tests/test_tilerace_submissions_contract.py`. Web review queue: `web-app/src/components/tilerace/controls/{SubmissionsPanel,SubmissionRow}.tsx`.
- WOM player snapshots + clan ranking (api-backend): hourly `app/services/ranking_service/` — `service.py` fetches WOM `/groups/{id}/bulk-hiscores`, `_parse.py` (`build_snapshot_rows`) splits each entry into the config-filtered ranking input and the persisted `player_snapshots` row (which also keeps `overall` XP and the `player.ehp`/`player.ehb` WOM computes per account type, migration `0059`), `scoring.py` + `_skill_defaults.py`/`_boss_defaults.py` do the points math. Model `app/db/models/ranking.py`. Read surfaces: `GET /members/me/snapshot` (`app/routers/members/snapshot.py`) and `app/routers/ranking/`. Web: `web-app/src/components/profile/WomSnapshotCard.tsx` (overall XP, top 3 skills, top 5 bosses, ironman EHP/EHB) via `useMySnapshot`, type `src/types/members.ts` `PlayerSnapshot`; the same map feeds `SkillsCard.tsx`, `presets.ts` and `YearlyGoalsCard.tsx`, all of which key by metric name so the extra `overall` key is inert.
- Root launcher: `./run` / `.\run.ps1` are shims that hand `scripts/run.py` (PEP 723, `uv run --script`, Textual) to uv. `scripts/launcher/catalog.py` holds every menu entry (run: dev/staging/prod, test: the run-tests lanes, maintenance: the `scripts/` jobs); `stack.py` is the ported infisical + docker compose invocation that replaced `rundev|runprod|runstaging.{sh,ps1}`; `cli.py` parses `./run <action> [choice] [--flag] [-- extra]` plus `--list`/`--print`; `app.py`+`panel.py` are the Textual menu, `fallback.py` the numbered prompt, `runner.py` the exec. Menu exits BEFORE the command runs, so jobs get a real TTY. `run-tests.sh` stays at the root (CI + rulesets pin it); `run-tests.ps1` moved to `scripts/`. Reference: `scripts/README.md`. Adding a job: `.claude/rules/launcher-actions.md` (PAG POLICY).
- Host maintenance / scheduled jobs: `scripts/README.md` is the operator index for `scripts/`. Docker disk reclamation is `scripts/docker-cleanup.sh` (dangling images older than `IMAGE_UNTIL`, BuildKit cache by `CACHE_UNTIL` then capped at `CACHE_MAX_SPACE`; `--dry-run` supported), scheduled daily 04:00 by `scripts/systemd/docker-cleanup.{service,timer}` (units live in the repo, installed by copying to `/etc/systemd/system/`; runs as `salt:docker`). The host also carries unrelated stacks (traefik, grafana, prometheus, copyparty, dinkproxy) and their named data volumes, so any docker cleanup here must never prune volumes, containers, or tagged images.
